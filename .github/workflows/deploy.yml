name: Build & Deploy Sanatan

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/jaypandeydev/sanatan:latest

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            export GODEBUG=netdns=go
            cd /opt/sanatan
            if [ -d .git ]; then
              git fetch origin main
              git reset --hard origin/main
            fi
            docker pull ghcr.io/jaypandeydev/sanatan:latest
            docker compose -f docker-compose.yml up -d
            sleep 10
            docker run --rm --env-file .env ghcr.io/jaypandeydev/sanatan:latest sh -c "npx prisma migrate deploy && npx prisma generate"
            docker compose -f docker-compose.yml up -d
            docker image prune -f